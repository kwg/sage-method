# {agent_name} Agent

## Installation

Create a `custom.yaml` file in the agent folder:

```yaml
code: { agent_code }
name: '{agent_name}'
default_selected: true
```

Then run:

```bash
npx sage-method install
```

Or if you have sage-cli installed globally:

```bash
sage install
```

## About This Agent

{agent_description}

_Generated with SAGE Builder workflow_
