# Edit Agent Workflow

**Status**: Not Available in SAGE Standalone Mode

This workflow would allow dynamic editing of agent configurations.

## SAGE Alternative

Edit agent files directly in `sage/agents/` directory.
